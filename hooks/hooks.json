{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before writing or editing code in a Vercel project, run these checks. IMPORTANT: Some rules are conditional — only apply them when the relevant package exists in the project's package.json.\n\n── ALWAYS APPLY (all Vercel projects) ──\n\n(1) SUNSET PACKAGES: Are you using `@vercel/postgres` or `@vercel/kv`? These are sunset — use `@neondatabase/serverless` and `@upstash/redis` instead.\n\n── NEXT.JS 16+ ONLY (apply when package.json has \"next\" >= 16) ──\n\nCheck package.json for `\"next\"` in dependencies or devDependencies. If the version is >= 16, apply rules 2–6. If `next` is not present or the version is < 16, SKIP rules 2–6.\n\n(2) PROXY FILE RENAME: Are you creating or editing `middleware.ts`? In Next.js 16 it has been renamed to `proxy.ts`.\n\n(3) TURBOPACK CONFIG: Are you putting turbopack config under `experimental.turbopack` in next.config? In Next.js 16, turbopack config is top-level (e.g., `turbopack: { rules: {} }`).\n\n(4) CACHE COMPONENTS: Are you using `'use cache'` at the top of a Client Component (a file with `'use client'`)? `'use cache'` only works in Server Components and Server Actions. Move the cached logic to a separate Server Component or use `cacheLife`/`cacheTag` in a Server Action.\n\n(5) ASYNC REQUEST APIS: In Next.js 16, `cookies()`, `headers()`, and `params` are async. Are you calling them synchronously (e.g., `const cookieStore = cookies()`)? You must `await` them: `const cookieStore = await cookies()`.\n\n(6) IMAGE COMPONENT: Are you using `<img>` tags in a Next.js project? Use `next/image` (`import Image from 'next/image'`) for automatic optimization, lazy loading, and CLS prevention.\n\n── AI SDK v6 ONLY (apply when package.json has \"ai\" package) ──\n\nCheck package.json for `\"ai\"` in dependencies or devDependencies. If present, apply rules 7–9. If `ai` is not in package.json, SKIP rules 7–9.\n\n(7) AI SDK TOOL SCHEMA: Are you using `parameters` or `result` in AI SDK tool definitions? These are deprecated in v6 — use `inputSchema`/`outputSchema` instead.\n\n(8) AI SDK AGENT CLASS: Are you manually implementing a tool-call while-loop with `generateText`? In AI SDK v6, use the `Agent` class instead, which handles the tool loop automatically with built-in `maxSteps` and error handling.\n\n(9) VERCEL WORKFLOW IMPORT: Are you importing from `@vercel/workflow` directly for AI agent patterns? Use `@vercel/workflow/ai` for `DurableAgent` and `World` imports."
          }
        ]
      }
    ]
  }
}
